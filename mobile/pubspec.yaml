name: curitiba_empreiteira_ponto
description: Aplicativo mobile de ponto facial da Curitiba Empreiteira.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+1
  connectivity_plus: ^6.0.3
  geolocator: ^12.0.0
  http: ^1.2.1
  intl: ^0.19.0
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

